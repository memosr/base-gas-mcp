# Private key of a FUNDED Base mainnet buyer wallet (holds USDC + a little ETH for gas).
# Each get_base_gas call spends real $0.001 USDC. 0x-prefixed, 64 hex chars.
BUYER_PRIVATE_KEY=

# Optional: override the x402 gas API URL.
# Defaults to https://base-gas-x402-production.up.railway.app/gas
TARGET_URL=

# Base Builder Code attached to the x402 payment payload for attribution.
# Defaults to bc_lhfd8zad if unset.
BUILDER_CODE=bc_lhfd8zad
